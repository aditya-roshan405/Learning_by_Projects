# ===================================================
# AI Resume Analyzer & Job Recommendation System
# requirements.txt
# ===================================================
# This file lists every external Python library the project needs.
# Anyone (including you, on a new machine) can install everything
# in one command:
#
#       pip install -r requirements.txt
#
# Pinning versions (using ==) makes the project "reproducible" —
# it will behave the same way on any computer, today or a year
# from now, even if newer library versions change behavior.
# ===================================================

# ---- Web App Framework ----
streamlit==1.38.0        # Builds the interactive web dashboard (Module 7)

# ---- Data Handling ----
pandas==2.2.2             # Reading/writing CSV files, tables of job roles & skills
numpy==1.26.4              # Numerical operations (used internally by pandas/sklearn)

# ---- Machine Learning / NLP ----
scikit-learn==1.5.1        # TF-IDF vectorizer + cosine similarity (Module 5)

# ---- File Parsing ----
pypdf==4.3.1                # Extract text from PDF resumes (Module 2)
python-docx==1.1.2          # Extract text from DOCX resumes (Module 2)

# ---- Visualization ----
plotly==5.23.0              # Match-score charts on the dashboard (Module 7)

# ---- Utilities ----
python-dotenv==1.0.1         # Load settings/secrets from a .env file safely
